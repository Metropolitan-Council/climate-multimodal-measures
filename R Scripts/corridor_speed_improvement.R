# corridor_speed_improvement
# Fuel Consumption Reduced (Gallons) = Corridor Distance (miles) × AADTno build [k1speed no build – (1 + EID) × k1speed build]
# Where:
# •	AADTno build = Annual average daily traffic under the no-build condition
# •	EID = Elasticity of induced VMT due to improved corridor speed; default value can be found in Table 22, with justification further explained the following section.
# •	K1 = running fuel use factor, in gallon/hour. The default assumes LD gasoline vehicles only: 000019137 × cruise-speed2 -0.0020660× cruise-speed +0.088916
# GHG Reduced (kg) = Fuel Consumption Reduced (Gallons) * 9.915

